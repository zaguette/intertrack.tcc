// Este arquivo existe apenas para evitar que o Prisma CLI falhe ao tentar
// interpretar um arquivo de configuração; ele não exporta nada significativo.
// Se você quiser um helper runtime para o Prisma Client, use `prisma.client.js`.

// Usar CommonJS aqui garante que o CLI do Prisma consiga parsear o arquivo
// mesmo em projetos ESM.

module.exports = {};
